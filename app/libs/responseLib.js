exports.generate = (response, message, status, data) => {

    let response = {
        response: err,
        message: message,
        status: status,
        data: data
    };

    return response;
}